{% extends 'base.html.twig' %}

{% block header %}
	<nav class="navbar navbar-expand-lg navbar-light bg-blue">
		<div class="container-fluid">
			<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="true" aria-label="Toggle navigation">
				<span class="navbar-toggler-icon"></span>
			</button>
			<div class="d-flex align-items-end justify-content-between">
				<img class="nav-img d-none d-md-block m-2" src="{{ asset('build/images/bicycle.png') }}"/>
				<span class="navbar-brand w-auto">Sell'aventure</span>
			</div>
			<div class="collapse navbar-collapse justify-content-center align-items-end" id="navbarSupportedContent">
				<ul class="navbar-nav w-75 d-flex justify-content-around">
					<li class="nav-item d-flex align-items-end">
						<a class="nav-link p-0" aria-current="page" href="{{ path('index') }}">Voyages</a>
					</li>
					<li class="nav-item d-flex align-items-end">
						<a class="nav-link p-0" href="#">Cyclistes</a>
					</li>
					<li class="nav-item d-flex align-items-end">
						<a class="nav-link p-0" href="#">Carte</a>
					</li>
					{% if app.user %}
						<li class="nav-item d-flex align-items-end">
							<a class="nav-link p-0" href="{{ path('journeys_user', {cyclist: app.user.id}) }}">Mes voyages</a>
						</li>
						<li class="nav-item d-flex align-items-end">
							<a class="nav-link p-0" href="{{ path('app_logout') }}">Se déconnecter</a>
						</li>
						<li class="nav-item d-flex align-items-end">
							<span class="logged-user">{{ app.user.username }}</span>
						</li>
					{% else %}
						<li class="nav-item d-flex align-items-end">
							<a class="nav-link p-0" href="{{ path('app_login') }}">Se connecter</a>
						</li>
					{% endif %}
				</ul>
			</div>
		</div>
	</nav>
{% endblock %}
